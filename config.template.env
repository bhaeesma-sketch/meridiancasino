# =============================================================================
# CRYPTO CASINO PLATFORM - FULL CONFIGURATION TEMPLATE
# =============================================================================
# 
# ⚠️  LOOKING FOR SIMPLER? 
# Check: config.minimal.env - Only requires NOWPayments + one blockchain
# 
# This is the FULL template with all features.
# For minimal setup, use config.minimal.env instead.
# =============================================================================
# 
# SECURITY WARNING: This is a TEMPLATE file.
# 
# 1. Copy this file to: .env.local (for local development)
# 2. Copy this file to: .env.production (for production)
# 3. Fill in YOUR actual values (do NOT commit real values to git)
# 4. Add .env.* to .gitignore
# 5. Store production secrets in Vault/AWS Secrets Manager
#
# =============================================================================

# -----------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=3000
API_URL=http://localhost:3001
FRONTEND_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=casino_db
DB_USER=YOUR_DB_USER
DB_PASSWORD=YOUR_DB_PASSWORD

# Database SSL (production should use SSL)
DB_SSL=false

# Redis (for sessions, caching, rate limiting)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=YOUR_REDIS_PASSWORD

# -----------------------------------------------------------------------------
# WALLET CONFIGURATION - HOT WALLETS
# -----------------------------------------------------------------------------
# Ethereum Hot Wallet (for withdrawals)
HOT_WALLET_ETH_ADDRESS=0xYOUR_ETHEREUM_HOT_WALLET_ADDRESS
# Private key encrypted and stored in Vault, NOT here

# BSC Hot Wallet
HOT_WALLET_BSC_ADDRESS=0xYOUR_BSC_HOT_WALLET_ADDRESS

# TRON Hot Wallet
HOT_WALLET_TRON_ADDRESS=TYOUR_TRON_HOT_WALLET_ADDRESS

# Polygon Hot Wallet (if supported)
HOT_WALLET_POLYGON_ADDRESS=0xYOUR_POLYGON_HOT_WALLET_ADDRESS

# Hot Wallet Balance Limits (USD)
HOT_WALLET_MAX_BALANCE=50000
HOT_WALLET_MIN_BALANCE=10000

# -----------------------------------------------------------------------------
# WALLET CONFIGURATION - COLD WALLETS (MULTISIG)
# -----------------------------------------------------------------------------
# Ethereum Cold Wallet (3-of-5 multisig)
COLD_WALLET_ETH_ADDRESS=0xYOUR_ETHEREUM_COLD_WALLET_ADDRESS

# TRON Cold Wallet
COLD_WALLET_TRON_ADDRESS=TYOUR_TRON_COLD_WALLET_ADDRESS

# BSC Cold Wallet
COLD_WALLET_BSC_ADDRESS=0xYOUR_BSC_COLD_WALLET_ADDRESS

# Multisig Signer Addresses (for cold wallet operations)
# Format: comma-separated list
ADMIN_WALLET_ADDRESSES=0xAdmin1Address,0xAdmin2Address,0xAdmin3Address,0xAdmin4Address,0xAdmin5Address

# Multisig Threshold (e.g., 3-of-5)
MULTISIG_THRESHOLD=3
MULTISIG_TOTAL_SIGNERS=5

# -----------------------------------------------------------------------------
# NOWPAYMENTS CONFIGURATION
# -----------------------------------------------------------------------------
# Get these from: https://nowpayments.io/dashboard
NOWPAYMENTS_API_KEY=YOUR_NOWPAYMENTS_API_KEY
NOWPAYMENTS_IPN_SECRET=YOUR_NOWPAYMENTS_IPN_SECRET
NOWPAYMENTS_SANDBOX_MODE=true

# Webhook endpoint (your backend URL)
NOWPAYMENTS_WEBHOOK_URL=https://your-domain.com/webhooks/nowpayments

# IP Whitelist for NOWPayments webhooks
# Get from: https://documenter.getpostman.com/view/7907941/S1a32n38
NOWPAYMENTS_IP_WHITELIST=52.31.139.75,52.49.173.169,52.214.14.220

# -----------------------------------------------------------------------------
# BLOCKCHAIN NETWORK CONFIGURATION
# -----------------------------------------------------------------------------
# Ethereum RPC Endpoints
ETH_RPC_URL=https://mainnet.infura.io/v3/YOUR_INFURA_KEY
ETH_RPC_WS_URL=wss://mainnet.infura.io/ws/v3/YOUR_INFURA_KEY
ETH_CHAIN_ID=1

# BSC RPC Endpoints
BSC_RPC_URL=https://bsc-dataseed.binance.org/
BSC_RPC_WS_URL=wss://bsc-ws-node.nariox.org:443
BSC_CHAIN_ID=56

# TRON Network
TRON_NETWORK=mainnet
TRON_API_KEY=YOUR_TRONGRID_API_KEY

# Polygon RPC (if supported)
POLYGON_RPC_URL=https://polygon-rpc.com/
POLYGON_CHAIN_ID=137

# Required Confirmations (block confirmations before crediting)
REQUIRED_CONFIRMATIONS_ETH=12
REQUIRED_CONFIRMATIONS_BSC=20
REQUIRED_CONFIRMATIONS_TRON=20
REQUIRED_CONFIRMATIONS_POLYGON=12

# -----------------------------------------------------------------------------
# SECURITY CONFIGURATION
# -----------------------------------------------------------------------------
# JWT Configuration
JWT_SECRET=YOUR_JWT_SECRET_KEY_MIN_32_CHARS_LONG
JWT_EXPIRY_MINUTES=15
JWT_REFRESH_EXPIRY_DAYS=7

# Nonce Configuration
NONCE_EXPIRY_MINUTES=5

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Session Configuration
SESSION_SECRET=YOUR_SESSION_SECRET_KEY
SESSION_TIMEOUT_MINUTES=30
MAX_CONCURRENT_SESSIONS=3

# Encryption Keys (for sensitive data at rest)
ENCRYPTION_KEY=YOUR_32_CHAR_ENCRYPTION_KEY

# -----------------------------------------------------------------------------
# SECRETS VAULT CONFIGURATION
# -----------------------------------------------------------------------------
# HashiCorp Vault (if using)
VAULT_ADDR=https://vault.your-domain.com
VAULT_TOKEN=YOUR_VAULT_TOKEN
VAULT_MOUNT_PATH=secret/data/casino

# AWS Secrets Manager (if using)
AWS_REGION=us-east-1
AWS_SECRETS_MANAGER_ENABLED=false

# -----------------------------------------------------------------------------
# ADMIN PANEL CONFIGURATION
# -----------------------------------------------------------------------------
# Admin Panel Access (VPN-only endpoint)
ADMIN_PANEL_URL=https://admin.your-domain.com
ADMIN_PANEL_ENABLED=true

# IP Allowlist for Admin Panel (comma-separated)
ADMIN_IP_WHITELIST=YOUR_OFFICE_IP,YOUR_HOME_IP,YOUR_VPN_IP

# Admin 2FA Configuration
ADMIN_2FA_ISSUER=Casino Platform Admin
ADMIN_2FA_ENABLED=true

# Hardware Key Configuration
HARDWARE_KEY_ENABLED=true

# -----------------------------------------------------------------------------
# RNG SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
RNG_SERVICE_URL=http://rng-service:3002
RNG_SERVICE_SECRET=YOUR_RNG_SERVICE_SECRET

# Seed Rotation
RNG_SEED_ROTATION_GAMES=1000
RNG_SEED_ROTATION_HOURS=24

# -----------------------------------------------------------------------------
# WITHDRAWAL CONFIGURATION
# -----------------------------------------------------------------------------
# Withdrawal Limits (USD)
WITHDRAWAL_MIN_AMOUNT_ETH=10
WITHDRAWAL_MIN_AMOUNT_TRON=5
WITHDRAWAL_MIN_AMOUNT_BSC=10

WITHDRAWAL_MAX_AMOUNT_PER_DAY=5000
WITHDRAWAL_MAX_COUNT_PER_DAY=10
WITHDRAWAL_MIN_INTERVAL_MINUTES=60

# Auto-approval limits
WITHDRAWAL_AUTO_APPROVAL_LIMIT=100
WITHDRAWAL_AUTO_APPROVAL_DELAY_HOURS=2

# Manual approval thresholds
WITHDRAWAL_MANUAL_APPROVAL_THRESHOLD=1000

# -----------------------------------------------------------------------------
# DEPOSIT CONFIGURATION
# -----------------------------------------------------------------------------
# Deposit Limits (USD)
DEPOSIT_MIN_AMOUNT=1
DEPOSIT_MAX_AMOUNT=50000

# Deposit expiry (minutes)
DEPOSIT_EXPIRY_MINUTES=30

# -----------------------------------------------------------------------------
# ANTI-FRAUD CONFIGURATION
# -----------------------------------------------------------------------------
# Bot Detection
BOT_DETECTION_ENABLED=true
BOT_DETECTION_THRESHOLD=0.7

# Velocity Checks
MAX_BETS_PER_MINUTE=30
MIN_BET_INTERVAL_MS=200

# Account Freeze Thresholds
AUTO_FREEZE_BOT_SCORE=0.9
AUTO_FREEZE_RTP_DEVIATION=5

# -----------------------------------------------------------------------------
# MONITORING & ALERTS
# -----------------------------------------------------------------------------
# Prometheus
PROMETHEUS_ENABLED=true
PROMETHEUS_PORT=9090

# Alerting
ALERT_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK
ALERT_EMAIL_FROM=noreply@your-domain.com
ALERT_EMAIL_TO=alerts@your-domain.com

# PagerDuty (for critical alerts)
PAGERDUTY_API_KEY=YOUR_PAGERDUTY_KEY

# SMS Alerts (Twilio)
TWILIO_ACCOUNT_SID=YOUR_TWILIO_SID
TWILIO_AUTH_TOKEN=YOUR_TWILIO_TOKEN
TWILIO_PHONE_NUMBER=+1234567890
ALERT_PHONE_NUMBERS=+1234567890,+0987654321

# -----------------------------------------------------------------------------
# KYC/AML CONFIGURATION
# -----------------------------------------------------------------------------
# KYC Provider (e.g., Jumio, Onfido, Sumsub)
KYC_PROVIDER=jumio
KYC_API_KEY=YOUR_KYC_API_KEY
KYC_API_SECRET=YOUR_KYC_API_SECRET

# KYC Limits
ANONYMOUS_DEPOSIT_LIMIT=500
ANONYMOUS_WITHDRAWAL_LIMIT=500
KYC_LEVEL1_DEPOSIT_LIMIT=5000
KYC_LEVEL1_WITHDRAWAL_LIMIT=5000
KYC_LEVEL2_DEPOSIT_LIMIT=50000
KYC_LEVEL2_WITHDRAWAL_LIMIT=50000

# -----------------------------------------------------------------------------
# GEO-BLOCKING
# -----------------------------------------------------------------------------
# Blocked Countries (comma-separated ISO country codes)
BLOCKED_COUNTRIES=US,GB,FR

# Enable Geo-blocking
GEO_BLOCKING_ENABLED=true

# -----------------------------------------------------------------------------
# LOGGING CONFIGURATION
# -----------------------------------------------------------------------------
# Log Level: debug, info, warn, error
LOG_LEVEL=info

# ELK Stack
ELASTICSEARCH_URL=http://elasticsearch:9200
LOGSTASH_ENABLED=true

# -----------------------------------------------------------------------------
# EMAIL CONFIGURATION
# -----------------------------------------------------------------------------
# SMTP Configuration
SMTP_HOST=smtp.your-domain.com
SMTP_PORT=587
SMTP_USER=noreply@your-domain.com
SMTP_PASSWORD=YOUR_SMTP_PASSWORD
SMTP_FROM_NAME=Casino Platform
SMTP_FROM_EMAIL=noreply@your-domain.com

# -----------------------------------------------------------------------------
# SMART CONTRACT CONFIGURATION (if using)
# -----------------------------------------------------------------------------
# Contract Addresses
ESCROW_CONTRACT_ETH=0xYOUR_ESCROW_CONTRACT_ADDRESS
ESCROW_CONTRACT_BSC=0xYOUR_ESCROW_CONTRACT_ADDRESS

# Contract Admin
CONTRACT_ADMIN_ADDRESS=0xYOUR_CONTRACT_ADMIN_ADDRESS

# Emergency Pause
CONTRACT_PAUSE_ENABLED=true
CONTRACT_PAUSE_COOLDOWN_HOURS=48

# -----------------------------------------------------------------------------
# FEATURE FLAGS
# -----------------------------------------------------------------------------
FEATURE_DEPOSITS_ENABLED=true
FEATURE_WITHDRAWALS_ENABLED=true
FEATURE_GAMES_ENABLED=true
FEATURE_BONUSES_ENABLED=false
FEATURE_AFFILIATES_ENABLED=false

# -----------------------------------------------------------------------------
# TESTING CONFIGURATION
# -----------------------------------------------------------------------------
# Test Wallets (for development only)
TEST_WALLET_ADDRESS=0xTestWalletAddress
TEST_PRIVATE_KEY=YOUR_TEST_PRIVATE_KEY

# Test Mode (disable real transactions)
TEST_MODE=false

# =============================================================================
# END OF CONFIGURATION TEMPLATE
# =============================================================================
# 
# REMEMBER:
# - Never commit real secrets to git
# - Use environment-specific files (.env.development, .env.production)
# - Store production secrets in Vault/AWS Secrets Manager
# - Rotate secrets regularly
# - Use strong, randomly generated secrets
#
# =============================================================================

